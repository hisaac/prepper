[project]
name = "prepper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
	{ name = "Isaac Halvorson", email = "hello@hisaac.net" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
prepper = "prepper:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"
